package xochitl.interview.meta.exercises.leetcode.oracle;

public class Interview {

    /*public static void main(String[] args) {
        int[] arr1 = {48, 7, 8,  4, 13, 15, 21};
        int[] arr2 = {12, 19, 17,32, 19, 26, 49};

        // Expected output (13,12), (48,49)
        int minimaDiferencia = 0;
        ArrayList<Integer[]> array = new ArrayList<>();

        for(int i = 0; i < arr1.length; i++)    {
            for(int j = 0; j < arr2.length; j++)    {
                int diferencia = Math.abs(arr1[i] - arr2[j]);
                if(direncia < minimaDiferencia)    {
                    minimaDiferencia = diferencia;
                    array = new ArrayList<>();
                    array.add(new int[arr1[i], arr2[j]]);
                }
                else  if(direncia == minimaDiferencia)   {
                    array.add(new int[arr1[i], arr2[j]]);
                }
            }
        }

        return array;

        //4,7,8,13,15,21,48
        //12,17,19,19,26,32,49

        int i = 0;
        int j = 0;
        int minimaDiferencia = -1;
        ArrayList<Integer[]> array = new ArrayList<>();
        while(i < arr1.length && j < arr2.length)    {
            int dif = Math.abs(arr1[i] - arr2[j]);
            if(dif < minimaDiferencia)    {
                minimaDiferencia = diferencia;
            }
            else    {
                //agregar al array
            }
            if(arr1[i] < arr[j])    {
                i++;
            }
            else    {
                j++;
            }
        }

    }*/
}
